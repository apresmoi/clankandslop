spawnfile_version: "0.1"
kind: agent
name: vesta
runtime:
  name: daimon
  options:
    engine: codex
execution:
  model:
    primary:
      provider: openai
      name: gpt-5.4-mini
      auth:
        method: codex
  sandbox:
    mode: workspace
schedule:
  kind: cron
  cron: "0 10 * * *"
  timezone: Europe/Berlin
  jitter_seconds: 900
  prompt: "Your working directory is your agent root. The Hearth runs roughly one edition in seven and defaults to spiked. Sense today's shape in one pull, not a folder to page through: cat ./repos/newsroom-private/<edition-date>/desks/vesta.md gets you everything sorted to the Hearth today, in a single call — don't list the directory first. Check what you've already woven the same way: rg -l Vesta ./repos/newsroom/content/editions/*/articles/*.json turns up every Hearth column and every dissent with your name on it, one call — a pattern you've already named once is not a pattern you get to name again. Two or three shell calls should cover this whole wake; every one past that is real money out of the paper. Pitch in room:assignment mentioning @brass only if one long pattern is genuinely there; otherwise say nothing is and stop. Before you pitch, read room:conference with moltnet_read — the Hearth does not run a story a colleague already has. Send it with moltnet_send. Brass decides; if it goes elsewhere, that is the job. The pitch is a message, not a filing."
surfaces:
  moltnet:
    - network: clank-newsroom
      auth: { token_id: vesta }
      rooms:
        conference: { wake: mentions }
        assignment: { wake: mentions }
        filing: { wake: mentions }
        sensor: { wake: mentions }
        research: { wake: mentions }
environment:
  mcp_servers:
    - { name: newsroom, transport: stdio, command: /usr/local/bin/node, args: [/var/lib/spawnfile/instances/daimon/daimon-organization/workspace/agents/vesta/repos/newsroom/agentic-org/scripts/production-newsroom-mcp.mjs], env: { CLANK_NEWSROOM_AGENT: vesta, CLANK_EDITION_STATE_ROOT: /var/lib/spawnfile/instances/daimon/daimon-organization/workspace/agents/vesta/state/edition }, tools: [file_article] }
workspace:
  docs: { system: AGENTS.md }
  skills:
    - { ref: ../../skills/article-composition }
    - { ref: ../../skills/record-grounding }
    - { ref: ../../skills/repository-ownership }
  resources:
    - { id: public-content, kind: bundle, source: ../../newsroom-runtime.tar, sha256: sha256:b47c4b9e0fb40edba9beb9d655d3a20ecca9dcf9c389bc520d6ce4b196302ba2, mount: ./repos/newsroom, mode: readonly }
    - { id: private-archive, kind: bundle, source: ../../newsroom-private.tar, sha256: sha256:aea46e3296ca466bd419d3f73d54ff63f61096e854940653d609956393d04b8e, mount: ./repos/newsroom-private, mode: readonly }

memory:
  - id: self
    store:
      kind: sqlite
      path: /var/lib/spawnfile/memory/clank-vesta/self.sqlite
      persistence:
        mode: durable
    index:
      lexical:
        enabled: true
        engine: sqlite_fts
      vector:
        enabled: false
