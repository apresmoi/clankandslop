# Public content contracts

This directory owns public content formats, pure validation and website build
gates. Runtime services and new tool adapters belong in the private repository.
Keep reusable validation free of filesystem, network and process state; callers
supply trusted registries. Frozen archive compatibility must never relax new
reporter filings. Do not rewrite prose, bylines, citations or provenance to pass
validation. Tests belong beside the contract they cover.
